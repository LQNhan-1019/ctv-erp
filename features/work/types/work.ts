export type EmployeeOption = { id:string; employeeCode:string; fullName:string; businessUnitId:string|null; businessUnitName:string; directReport:boolean; editable:boolean; dailyEditable:boolean };
export type WorkCapabilities = { view:boolean; manage:boolean; selfManage:boolean; approve:boolean };
export type WorkCategory = { id:string; businessUnitId:string|null; businessUnitName:string|null; code:string; name:string; description:string|null; active:boolean; catalogItems:number };
export type WorkCatalog = { id:string; businessUnitId:string|null; businessUnitName:string|null; categoryId:string|null; categoryName:string|null; code:string; name:string; details:string|null; defaultQuantity:number; defaultWeight:number; active:boolean };
export type EvidenceFile = { id:string; fileName:string; contentType:string; fileSize:number; createdAt:string };
export type DailyUpdate = { id:string; workDate:string; quantity:number; progressPercent:number|null; details:string|null; blockers:string|null; evidenceUrl:string|null; updatedAt:string; evidenceFiles:EvidenceFile[] };
export type WorkItem = { id:string; catalogItemId:string|null; title:string; details:string|null; itemType:'PLANNED'|'AD_HOC'; targetQuantity:number; weight:number; targetPoints:number; completedQuantity:number; completedPoints:number; remainingPoints:number; completionPercent:number; priority:'LOW'|'NORMAL'|'HIGH'|'URGENT'; status:'OPEN'|'IN_PROGRESS'|'DONE'|'CANCELLED'; dueDate:string|null; sortOrder:number; assignedByUserId:string|null; assignedByName:string|null; assignedAt:string|null; dailyUpdates:DailyUpdate[] };
export type ApprovalAction = { id:string; action:string; actorUserId:string; actorName:string; comments:string|null; actedAt:string };
export type WorkPlan = { id:string; employeeId:string; employeeCode:string; employeeName:string; businessUnitId:string|null; businessUnitName:string; month:string; approverEmployeeId:string|null; approverName:string|null; status:'DRAFT'|'SUBMITTED'|'APPROVED'|'REJECTED'; notes:string|null; targetPoints:number; plannedCompletedPoints:number; adHocCompletedPoints:number; completedPoints:number; completionPercent:number; submittedAt:string|null; approvedAt:string|null; items:WorkItem[]; approvalHistory:ApprovalAction[] };
export type DashboardEmployee = { employeeId:string; employeeCode:string; employeeName:string; businessUnitName:string; planStatus:string; totalItems:number; completedItems:number; targetPoints:number; plannedCompletedPoints:number; adHocCompletedPoints:number; completedPoints:number; completionPercent:number };
export type WorkDashboard = { month:string; employees:number; plans:number; submitted:number; approved:number; overdueItems:number; targetPoints:number; plannedCompletedPoints:number; adHocCompletedPoints:number; completedPoints:number; completionPercent:number; employeeResults:DashboardEmployee[] };
export type CategoryInput = { businessUnitId:string|null; code:string|null; name:string; description:string|null; active:boolean };
export type CatalogInput = { businessUnitId:string|null; categoryId?:string|null; code:string; name:string; details:string|null; defaultQuantity:number; defaultWeight:number; active:boolean };
export type WorkItemInput = { catalogItemId:string|null; title:string; details:string|null; itemType:'PLANNED'|'AD_HOC'; targetQuantity:number; weight:number; priority:'LOW'|'NORMAL'|'HIGH'|'URGENT'; status:'OPEN'|'IN_PROGRESS'|'DONE'|'CANCELLED'; dueDate:string|null; sortOrder:number };
export type AssignmentInput = { employeeId:string; month:string; title:string; details:string|null; targetQuantity:number; weight:number; priority:'LOW'|'NORMAL'|'HIGH'|'URGENT'; dueDate:string|null };
export type WorkImportInput = { employeeId:string; month:string; sheetName:string|null; filename:string; workbookBase64:string; replaceExisting:boolean };
export type WorkImportRow = { rowNumber:number; workGroup:string|null; title:string; targetQuantity:number|null; weight:number|null; itemType:'PLANNED'|'AD_HOC'; dailyEntries:number; completedQuantity:number; valid:boolean; errors:string[] };
export type WorkImportPreview = { filename:string; sheetName:string; month:string; totalRows:number; validRows:number; invalidRows:number; rows:WorkImportRow[]; warnings:string[] };
export type WorkImportResult = { planId:string; sheetName:string; month:string; importedItems:number; importedDailyUpdates:number; replacedExisting:boolean; message:string };
export type CatalogImportInput = { businessUnitId:string|null; sheetName:string|null; filename:string; workbookBase64:string; replaceExisting:boolean };
export type CatalogImportPreview = { filename:string; sheetName:string; totalRows:number; validRows:number; invalidRows:number; rows:Array<{rowNumber:number;workGroup:string;title:string;defaultQuantity:number|null;defaultWeight:number|null;valid:boolean;errors:string[]}>; warnings:string[] };
export type CatalogImportResult = { sheetName:string; importedCategories:number; importedItems:number; replacedExisting:boolean; message:string };
